{
  "name": "keyscroll-docs",
  "version": "0.1.0",
  "description": "Docs and example for keyscroll.",
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -cw",
    "dev": "npm run watch",
    "start": "npm run dev",
    "g": "gh-pages --help",
    "publish:gh-pages": "gh-pages -d ./dist",
    "publish:gh-pages:ci": "gh-pages -d ./dist",
    "lint": "echo No linting for docs.",
    "test": "echo No tests for docs.",
    "publish": "npm run publish:gh-pages"
  },
  "dependencies": {
    "@smoovy/scroller": "^0.1.13",
    "keyscroll": "file:../keyscroll"
  },
  "devDependencies": {
    "rollup-plugin-generate-html-template": "^1.6.1"
  }
}
